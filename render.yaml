services:
  - type: web
    name: food-delivery-order-api
    runtime: node
    rootDir: apps/api
    buildCommand: npm install ; npm run build
    startCommand: npm run start
    plan: free
    envVars:
      - key: PORT
        value: 4000